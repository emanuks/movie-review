<%= form_with(model: movie) do |form| %>
  <% if movie.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(movie.errors.count, "error") %> prohibited this movie from being saved:</h2>

      <ul>
        <% movie.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>



  <div class="flex flex-col gap-3 mb-3">
    <%= form.label :name %>
    <%= form.text_field :name, class: "rounded h-10 border-gray-200 focus:border-blue-500" %>

    <%= form.label :description %>
    <%= form.text_area :description, class: "rounded h-36 border-gray-200 focus:border-blue-500" %>
  </div>

  <div>
    <%= form.submit "Submit", class: "px-2 py-1 text-center rounded bg-blue-500 font-bold text-white hover:opacity-75 mt-5 cursor-pointer" %>
  </div>
<% end %>
